{% extends "layout.html.twig" %}

{% block title %}Annuaire de Confiance - SnapFit{% endblock %}

{% block content %}
<div class="container mt-5">
    <div class="text-center mb-5">
        <h1 class="display-4 fw-bold">Annuaire de Confiance</h1>
        <p class="lead text-muted">Consultez notre base de données des sites éco-responsables et évitez les pièges.</p>
    </div>

    <!-- Alert Success Suggestion -->
    {% if app.request.get('success') %}
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <strong>Merci !</strong> Votre suggestion a bien été reçue. Notre équipe va l'analyser prochainement.
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    {% endif %}

    <div class="row">
        <!-- Liste des sites Éco -->
        <div class="col-md-6 mb-4">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-header bg-success text-white py-3">
                    <h3 class="mb-0"><i class="fas fa-leaf me-2"></i> Sites Éco-responsables</h3>
                </div>
                <div class="card-body">
                    <ul class="list-group list-group-flush">
                        {% for site in sitesEco %}
                        <li class="list-group-item d-flex justify-content-between align-items-center py-3">
                            <div>
                                <h6 class="mb-0 fw-bold">{{ site.nom }}</h6>
                                <small class="text-muted">{{ site.urlRacine }}</small>
                            </div>
                            <span class="badge bg-success rounded-pill">Vérifié Eco</span>
                        </li>
                        {% else %}
                        <li class="list-group-item text-center text-muted">Aucun site éco répertorié pour le moment.</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>

        <!-- Liste des sites Scam -->
        <div class="col-md-6 mb-4">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-header bg-danger text-white py-3">
                    <h3 class="mb-0"><i class="fas fa-exclamation-triangle me-2"></i> Sites Suspects (Scam)</h3>
                </div>
                <div class="card-body">
                    <ul class="list-group list-group-flush">
                        {% for site in sitesScam %}
                        <li class="list-group-item d-flex justify-content-between align-items-center py-3">
                            <div>
                                <h6 class="mb-0 fw-bold">{{ site.nom }}</h6>
                                <small class="text-muted">{{ site.urlRacine }}</small>
                            </div>
                            <span class="badge bg-danger rounded-pill">Signalé Scam</span>
                        </li>
                        {% else %}
                        <li class="list-group-item text-center text-muted">Aucun site frauduleux répertorié pour le moment.</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <!-- Formulaire de Suggestion -->
    <div class="row justify-content-center mt-5">
        <div class="col-md-8">
            <div class="card border-0 shadow p-4 bg-light">
                <div class="text-center mb-4">
                    <h3><i class="fas fa-paper-plane text-primary"></i> Proposer un nouveau site</h3>
                    <p class="text-muted">Aidez-nous à enrichir notre base de données.</p>
                </div>
                <form action="index.php?controleur=annuaire&methode=suggérer" method="POST">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label for="nom" class="form-label">Nom du site</label>
                            <input type="text" class="form-control" id="nom" name="nom" placeholder="Ex: Patagonia" required>
                        </div>
                        <div class="col-md-6">
                            <label for="url" class="form-label">URL (Domaine)</label>
                            <input type="text" class="form-control" id="url" name="url" placeholder="Ex: patagonia.com" required>
                        </div>
                        <div class="col-12">
                            <label class="form-label">Type de site</label>
                            <div class="d-flex gap-3">
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="statut" id="eco" value="eco" checked>
                                    <label class="form-check-label" for="eco">Eco-responsable</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="statut" id="scam" value="scam">
                                    <label class="form-check-label" for="scam">Suspect / Scam</label>
                                </div>
                            </div>
                        </div>
                        <div class="col-12 mt-4 text-center">
                            <button type="submit" class="btn btn-primary btn-lg px-5">Envoyer la proposition</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}
